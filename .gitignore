# Private notes (infra, VM, runner details) — never public
PRIVATE.md
private/

# Local AI review output (scripts/ai-review.sh): the latest review, and the
# running record of every review this checkout has run.
AI_REVIEW.md
.ai-review-log.md

# Secrets & environment
.env
.env.*
*.pem
*.key

# Editor / OS noise
.vscode/
.idea/
.DS_Store
*.swp

# Dependencies & build output (grows with the stack)
node_modules/
dist/
coverage/
